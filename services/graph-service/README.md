# Graph Service

负责知识图谱的节点、关系、时间线与冲突检测能力。

## MVP 状态

- 当前由 `apps/api-gateway` 返回 mock graph data
- 后续可对接 Neo4j 或其他图数据库

## 未来职责

- Entity / Relation CRUD
- Graph traversal
- Timeline 与 conflict query
