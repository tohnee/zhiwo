# UI Demo Render Check (work, 2026-04-27)

## Goal

- 查看 `work` 分支当前界面设计并完成一次可访问 demo 渲染验证。

## What was checked

1. UI layout code in `apps/web/src/App.tsx`:
   - notebook library 入口
   - workspace 三栏视图
   - studio cards（音频概览/源文导航/视频概览/思维导图/报告/闪卡/测验/信息图/数据表格）
   - debug 参数、connector sync、notes/reader 面板
2. Dev server render readiness:
   - start Vite dev server on port `4173`
   - verify HTTP 200 and root HTML is served

## Commands executed

```bash
pnpm --filter web dev --host 0.0.0.0 --port 4173
curl -I http://127.0.0.1:4173
curl -s http://127.0.0.1:4173 | head -n 20
```

## Result

- Dev server started successfully and served the app shell (`HTTP/1.1 200 OK`).
- Root document contains `#root` and `src/main.tsx` bootstrap script, indicating React app can be rendered by browser.

## Notes

- 当前运行环境未提供可用的 `browser_container` 工具，因此本次无法直接附带页面截图。
- 如需可视化截图，建议在带浏览器容器能力的会话中执行同样命令并抓图。
